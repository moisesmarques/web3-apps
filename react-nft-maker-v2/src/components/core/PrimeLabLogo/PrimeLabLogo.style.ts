import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const StyledLogo = styled(Box)`
  .left-sections {
    position: relative;
    overflow-x: hidden;
    z-index: 1;
    margin-top: 50px;
    margin-left: 20px;
  }
`;
